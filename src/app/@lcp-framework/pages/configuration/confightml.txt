<div class="mb-5">
  <div class="mt-3 flex flex-wrap border-b border-white-light dark:border-[#191e3a]">
    <a
      *ngFor="let tab of tabs"
      href="javascript:;"
      class="-mb-[1px] flex items-center border border-transparent p-3.5 py-2 outline-none transition duration-300 hover:text-danger"
      [ngClass]="{ '!border-white-light !border-b-white text-danger dark:!border-[#191e3a] dark:!border-b-black': currentTab === tab.name }"
      (click)="switchTab(tab.name)"
    >
      {{ tab.name }}
    </a>
  </div>

  <div class="flex-1 pt-5 text-sm">
    <ng-container *ngFor="let tab of tabs">
      <div *ngIf="currentTab === tab.name">
        <form (ngSubmit)="submitForm()" #form="ngForm">
          <div *ngFor="let config of tab.configurations">
            <div [ngSwitch]="config.config_field_type">
              <!-- Text Input -->
              <div *ngSwitchCase="'text'">
                <label [for]="config.config_key" class="block text-sm font-medium text-gray-700 dark:text-gray-200">{{ config.config_key | titlecase }}</label>
                <input
                  [id]="config.config_key"
                  type="text"
                  [(ngModel)]="config.config_value"
                  [name]="config.config_key"
                  class="form-input mt-1 block w-full rounded-md border-gray-300 dark:border-gray-600 dark:bg-gray-800"
                  required
                />
              </div>

              <!-- File Upload -->
              <div *ngSwitchCase="'file'">
                <label [for]="config.config_key" class="block text-sm font-medium text-gray-700 dark:text-gray-200">{{ config.config_key | titlecase }}</label>
                <input
                  [id]="config.config_key"
                  type="file"
                  (change)="onFileChange($event, config.config_key)"
                  [name]="config.config_key"
                  class="form-input mt-1 block w-full rounded-md border-gray-300 dark:border-gray-600 dark:bg-gray-800"
                />
              </div>

              <!-- Other input types remain the same -->

              <!-- Multi Select -->
              <!-- change multi_select to gridpaginationselect -->
              <div *ngSwitchCase="'multi_select'">
                <label [for]="config.config_key" class="block text-sm font-medium text-gray-700 dark:text-gray-200">{{ config.config_key | titlecase }}</label>

                <ng-select
                  [multiple]="true"
                  [id]="config.config_key"
                  name="`${config.config_key}`"
                  appDynamicFontSize="body"
                  class="custom-multiselect"
                  [clearable]="false"
                  placeholder="Select an option"
                  [ngModel]="config.config_value"
                >
                  @for (item of gridpaginationdropdownList; track item) {
                  <ng-option [value]="item">{{ item }}</ng-option>
                  }
                </ng-select>
              </div>

              <!-- Number Input -->
              <div *ngSwitchCase="'number'">
                <label [for]="config.config_key" class="block text-sm font-medium text-gray-700 dark:text-gray-200">{{ config.config_key | titlecase }}</label>
                <input
                  [id]="config.config_key"
                  type="number"
                  [ngModel]="config.config_value"
                  [name]="config.config_key"
                  class="form-input mt-1 block w-full rounded-md border-gray-300 dark:border-gray-600 dark:bg-gray-800"
                  required
                />
              </div>

              <!-- Checkbox -->
              <div *ngSwitchCase="'checkbox'">
                <label class="inline-flex items-center text-sm font-medium text-gray-700 dark:text-gray-200">
                  <input
                    type="checkbox"
                    [ngModel]="config.config_value"
                    [name]="config.config_key"
                    class="form-checkbox h-4 w-4 border-gray-300 text-red-500 dark:border-gray-600 dark:bg-gray-800"
                  />
                  <span class="ml-2">{{ config.config_key | titlecase }}</span>
                </label>
              </div>

              <!-- Single Select -->
              <!-- change select to time -->
              <div *ngSwitchCase="'select'">
                <label [for]="config.config_key" class="block text-sm font-medium text-gray-700 dark:text-gray-200">{{ config.config_key | titlecase }}</label>
                <input
                  type="time"
                  [ngModel]="config.config_value"
                  step="60"
                  [name]="config.config_key"
                  class="form-input mt-1 block w-full rounded-md border-gray-300 dark:border-gray-600 dark:bg-gray-800"
                />
              </div>
            </div>
          </div>
          <button type="submit" class="btn btn-primary mt-5">Submit</button>
        </form>
      </div>
    </ng-container>
  </div>
</div>
